using UnityEngine;

namespace Core
{
    public class GameInstance : MonoBehaviour
    {
        [SerializeField] private float zxc;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
